#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main8(int argc, char *argv[]) {
	
	int i;
	float x;
	char c;
	scanf("%3d %5f %c", &i, &x, &c);
	printf("%3d %5f %c", i, x, c);
	
	
	return 0;
}
